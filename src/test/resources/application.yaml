ktor:
  application:
    modules:
      - ru.planner.ApplicationKt.module
  deployment:
    port: 8080

jwt:
  domain: "https://planner.example.com"
  audience: "planner-trainer"
  realm: "Planner API"
  secret: "test-secret-must-be-at-least-32-chars-long"

postgres:
  url: "jdbc:postgresql://localhost:5439/planner"
  user: planner
  password: planner
